import styled from "styled-components";

export const DeleteBut = styled.button`
    width: 4vw;
    background: none;
    border: none;
    display: flex;
    align-items: center;
    justify-content: center;

    cursor: pointer;
`

export const DelImg = styled.img`
    width: 5vw;

    @media (max-width: 520px) {
        width: 10vw;
    }
`